const Navbar = () => {
  return (
    <nav className="flex justify-end m-6">
      <span className="text-2xl underline cursor-pointer transition-all duration-500 hover:text-gray-600 hover:no-underline">
        Sign out
      </span>
    </nav>
  );
};

export default Navbar;
